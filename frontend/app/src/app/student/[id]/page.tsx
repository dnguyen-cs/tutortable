export default function StudentPage({ params } : { params : { id: string }}) {
    // const student = await fetch(`${apiURL}/api/students/${params.id}`);

    return (
        <div>
            <h1>Student Page</h1>
        </div>
    )
}